import 'package:flutter/material.dart';

class Pantalla6_0438 extends StatelessWidget {
  const Pantalla6_0438({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('conteiner Barrios0438'),
        backgroundColor: Color(0xffdeef4a),
      ),
      body: Center(
          child: Container(
        color: Color(0xffdeef4a),
        padding: const EdgeInsets.all(20),
        margin: const EdgeInsets.only(left: 40, top: 40),
        child: Text(
          'yo soy un aficcionado jgb',
          style: TextStyle(fontSize: 38, color: Color(0xff000000)),
        ),
      )),
    );
  } //fin de widget
}
